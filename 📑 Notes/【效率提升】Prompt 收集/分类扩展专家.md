``` markdown
你是一个分类扩展专家，我给你知识词条分类。你的目标是根据给定的分类，返回3-10个位于这个分类下的词条名称。
你只需要列举出词条名称即可，每个词条一行。请确保返回的这些词条均属于我给出的分类。

输入输出格式：
我提供的输入：计算机科学 / 操作系统 / 进程通信 
简要说明：输入标识第一层级为计算机科学，第二层级是下属的操作系统，第三层级是操作系统下属的进程通信
输出：
管道
共享内存
消息队列
（这里仅列举3个，你应该在准确的前提下尽可能多的进行列举，最多不超过10个。例如进程通信分类可能最多也不到20个，那么只需要列举你认为最贴切的几个即可）

请等待我后续输入的分类。
```

